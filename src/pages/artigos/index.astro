---
// src/pages/artigos/index.astro
import Layout from '../../layouts/Base.astro';

const base = import.meta.env.BASE_URL;

// All articles — add new articles here
const articles = [
    {
        title: "Santo Alberto Magno: Bispo e Doutor da Igreja",
        subtitle: "O grande mestre dominicano e mentor de São Tomás de Aquino",
        author: "Katholikos",
        image: "assets/st_albert_magnus.jpg",
        link: "santo-alberto-magno",
        tags: ["Santos", "Dominicanos", "Doutores da Igreja"],
        date: "15 de Novembro",
        summary: "Conheça a vida do grande mestre dominicano que recebeu o título de 'Magno'. Mentor de São Tomás de Aquino, foi canonizado em 1931 e declarado Padroeiro dos cultores de Ciências naturais por Pio XII.",
    },
];
---

<Layout title="Artigos — Reflexões e Estudos">
    <div class="max-w-6xl mx-auto">
        <!-- Header -->
        <div class="mb-12">
            <div class="inline-block mb-6">
                <h2 class="text-sm font-bold text-blue-400 uppercase tracking-wider">Publicações</h2>
            </div>
            <h1 class="text-4xl md:text-5xl font-serif font-bold text-white mb-4">Artigos</h1>
            <p class="text-xl text-gray-300 max-w-3xl">
                Reflexões, biografias de santos e estudos sobre a fé católica para aprofundar seu conhecimento e enriquecer sua vida espiritual.
            </p>
        </div>

        <!-- Stats -->
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-12">
            <div class="bg-gray-900 border border-gray-800 rounded-lg p-5 text-center">
                <p class="text-3xl font-bold text-blue-400">{articles.length}</p>
                <p class="text-sm text-gray-400 mt-1">Artigos Publicados</p>
            </div>
            <div class="bg-gray-900 border border-gray-800 rounded-lg p-5 text-center">
                <p class="text-3xl font-bold text-blue-400">{[...new Set(articles.map(a => a.author))].length}</p>
                <p class="text-sm text-gray-400 mt-1">Autores</p>
            </div>
            <div class="bg-gray-900 border border-gray-800 rounded-lg p-5 text-center">
                <p class="text-3xl font-bold text-blue-400">{[...new Set(articles.flatMap(a => a.tags))].length}</p>
                <p class="text-sm text-gray-400 mt-1">Categorias</p>
            </div>
            <div class="bg-gray-900 border border-gray-800 rounded-lg p-5 text-center">
                <p class="text-3xl font-bold text-green-400">✦</p>
                <p class="text-sm text-gray-400 mt-1">Conteúdo Católico</p>
            </div>
        </div>

        <!-- Article List -->
        <div class="space-y-8 mb-16">
            {articles.map((article) => (
                <article class="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden hover:border-gray-700 transition group">
                    <div class="grid grid-cols-1 md:grid-cols-4 gap-0">
                        <!-- Image -->
                        <div class="md:col-span-1">
                            <a href={`${base}artigos/${article.link}`}>
                                <img
                                    src={article.image.startsWith('http') ? article.image : `${base}${article.image}`}
                                    alt={article.title}
                                    class="w-full h-64 md:h-full object-cover group-hover:scale-105 transition duration-500"
                                />
                            </a>
                        </div>

                        <!-- Details -->
                        <div class="md:col-span-3 p-6 md:p-8 flex flex-col justify-between">
                            <div>
                                <!-- Tags -->
                                <div class="flex flex-wrap gap-2 mb-4">
                                    {article.tags.map((tag: string) => (
                                        <span class="bg-blue-900 text-blue-300 text-xs font-semibold px-3 py-1 rounded-full">
                                            {tag}
                                        </span>
                                    ))}
                                </div>

                                <!-- Title -->
                                <a href={`${base}artigos/${article.link}`} class="block">
                                    <h2 class="text-2xl md:text-3xl font-serif font-bold text-white mb-2 group-hover:text-blue-400 transition">
                                        {article.title}
                                    </h2>
                                </a>
                                <p class="text-gray-400 font-serif italic mb-1">{article.subtitle}</p>
                                <p class="text-sm text-gray-500 mb-4">
                                    por <span class="text-gray-300 font-semibold">{article.author}</span>
                                    {article.date && (
                                        <span class="ml-2 text-gray-600">• Festa: {article.date}</span>
                                    )}
                                </p>

                                <!-- Summary -->
                                <p class="text-gray-300 leading-relaxed mb-6">
                                    {article.summary}
                                </p>
                            </div>

                            <!-- Actions -->
                            <div class="flex flex-wrap items-center gap-4">
                                <a
                                    href={`${base}artigos/${article.link}`}
                                    class="bg-blue-600 text-white px-6 py-3 rounded-lg font-semibold hover:bg-blue-700 transition text-sm"
                                >
                                    Ler Artigo Completo →
                                </a>
                            </div>
                        </div>
                    </div>
                </article>
            ))}
        </div>

        <!-- Coming soon -->
        <section class="bg-gray-900 border border-gray-800 border-dashed rounded-xl p-12 text-center mb-12">
            <p class="text-4xl mb-4">✍️</p>
            <h3 class="text-xl font-serif font-bold text-white mb-3">Mais artigos em breve</h3>
            <p class="text-gray-400 max-w-md mx-auto">
                Estamos preparando novos artigos sobre santos, teologia e espiritualidade católica. Volte regularmente para novas publicações.
            </p>
        </section>

        <!-- Why Read -->
        <section class="mb-12">
            <h2 class="text-3xl font-serif font-bold text-white mb-8">Nossos Temas</h2>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                <div class="bg-gray-900 border border-gray-800 p-6 rounded-lg">
                    <h3 class="text-blue-400 font-bold text-lg mb-4">✦ Vidas de Santos</h3>
                    <p class="text-gray-300 leading-relaxed">
                        Biografias e reflexões sobre os homens e mulheres que viveram heroicamente as virtudes cristãs e são modelos para nossa caminhada de fé.
                    </p>
                </div>
                <div class="bg-gray-900 border border-gray-800 p-6 rounded-lg">
                    <h3 class="text-blue-400 font-bold text-lg mb-4">✦ Teologia e Doutrina</h3>
                    <p class="text-gray-300 leading-relaxed">
                        Estudos acessíveis sobre os ensinamentos da Igreja, o Magistério e as verdades da fé católica para aprofundar sua compreensão.
                    </p>
                </div>
                <div class="bg-gray-900 border border-gray-800 p-6 rounded-lg">
                    <h3 class="text-blue-400 font-bold text-lg mb-4">✦ Espiritualidade</h3>
                    <p class="text-gray-300 leading-relaxed">
                        Reflexões sobre oração, vida interior e práticas espirituais para nutrir seu relacionamento com Deus no dia a dia.
                    </p>
                </div>
            </div>
        </section>
    </div>
</Layout>
