---
import Layout from '../../layouts/Base.astro';
import { getCategoryGroups } from '../../lib/content-browse';

const base = import.meta.env.BASE_URL;
const categories = getCategoryGroups();
---

<Layout title="Categorias">
    <div class="max-w-6xl mx-auto">
        <div class="mb-12">
            <div class="inline-block mb-6">
                <h2 class="text-sm font-bold text-blue-400 uppercase tracking-wider">Navegação</h2>
            </div>
            <h1 class="text-4xl md:text-5xl font-serif font-bold text-white mb-4">Categorias</h1>
            <p class="text-xl text-gray-300 max-w-3xl">
                Navegue por tipo de conteúdo para encontrar livros, artigos e recursos de forma mais rápida.
            </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-16">
            {categories.map((category) => (
                <a
                    href={`${base}categorias/${category.slug}`}
                    class="bg-gray-900 border border-gray-800 rounded-xl p-6 hover:border-gray-700 transition"
                >
                    <h2 class="text-2xl font-serif font-bold text-white mb-2 capitalize">{category.name}</h2>
                    <p class="text-gray-400 mb-4">{category.count} recurso(s)</p>
                    <p class="text-blue-400 font-semibold text-sm">Explorar categoria →</p>
                </a>
            ))}
        </div>
    </div>
</Layout>
